[project]
name = "lucabrary-python"
version = "0.1.0"
description = "Personal component library for lucathree"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.2.0",
]
